services:
  # Backend Service
  - type: web
    name: repo-chat-backend
    env: docker
    rootDir: backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FRONTEND_URL
        fromService:
          type: web
          name: repo-chat-frontend
          property: url

  # Frontend Service
  - type: web
    name: repo-chat-frontend
    env: docker
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: repo-chat-backend
          property: url
